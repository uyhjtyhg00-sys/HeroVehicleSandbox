# Function Comment Policy

Every gameplay-facing function added or substantially modified by a patch should include a short `// 기능:` comment.

The comment should answer one thing: what gameplay responsibility this function owns.

This keeps future patches consistent when movement, weapon, vehicle, HUD, and settings code are edited repeatedly.
